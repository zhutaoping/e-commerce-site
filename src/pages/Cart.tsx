const Cart = () => {
	return (
		<div>
			<h5 className="p-4 text-center">Cart</h5>
			{/* {documents && (
				<ProductList products={documents} handleLocalCart={handleLocalCart} />
			)} */}
		</div>
	);
};

export default Cart;
